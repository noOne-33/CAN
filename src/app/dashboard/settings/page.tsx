
import SettingsClient from './SettingsClient';

export const metadata = {
  title: 'Account Settings - CAN',
  description: 'Manage your account settings, preferences, and delete your account.',
};

export default function AccountSettingsPage() {
  return (
    <SettingsClient />
  );
}
