
import ProfileClient from './ProfileClient';

export const metadata = {
  title: 'My Profile - CAN',
  description: 'View and update your personal information.',
};

export default function ProfilePage() {
  return (
    <ProfileClient />
  );
}
